# Database Design

The database layer is prepared for prediction history and analytics. Main fields include email hash, sender domain, prediction label, confidence, risk reasons, and created timestamp.
