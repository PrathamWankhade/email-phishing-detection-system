# Workflow

User enters email → frontend sends request → API route receives request → controller calls services → AI engine preprocesses and scores → explanation engine returns reasons → frontend displays result.
